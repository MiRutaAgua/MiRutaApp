<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    >

    <RelativeLayout
        android:id="@+id/relativeLayout2"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:background="@android:color/black"
        android:gravity="center">

        <ImageView

            android:id="@+id/imageView_screen_zoom_photo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:foregroundGravity="center"
            android:src="@drawable/screen_unity_counter_imagen" />
        <!--- android:scaleType="matrix"  esto para zoom foto-->
    </RelativeLayout>

    <Button
        android:id="@+id/button_save_picture"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_marginBottom="20dp"
        android:layout_marginRight="90dp"
        android:background="@drawable/button_ok_screen_camera"
        app:layout_anchor="@+id/relativeLayout2"
        app:layout_anchorGravity="right|bottom" />

    <Button
        android:id="@+id/button_cancel_picture"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_marginBottom="20dp"
        android:layout_marginLeft="90dp"
        android:background="@drawable/button_cancel_screen_camera"
        app:layout_anchor="@+id/relativeLayout2"
        app:layout_anchorGravity="left|bottom" />
</android.support.design.widget.CoordinatorLayout>